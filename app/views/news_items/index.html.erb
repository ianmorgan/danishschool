<h1>Listing News </h1>

<table>
  <tr>
    <th>News date</th>
    <th>News text</th>
  </tr>

<% @news_items.each do |news_item| %>
  <tr>
    <td><%=h news_item.news_date %></td>
    <td><%=h news_item.trimmed_news_text %></td>
    <td><%= link_to 'Show', news_item %></td>
    <td><%= link_to 'Edit', edit_news_item_path(news_item) %></td>
    <td><%= link_to 'Destroy', news_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News', new_news_item_path %>
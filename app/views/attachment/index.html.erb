<h1>Listing pictures and attachments</h1>

<p class="pagehelp">
  This pages shows all attachments. These are files such as 
  pictures and pdf documents that can be 
  viewed or downloaded from the content and lessons pages.
  <br><br>
  <strong>Please note</strong> that Newsletters should be uploaded using the Newletters tab.       
</p>

<table>
  <tr>
    <th>Name</th>
    <th>Actions</th>
   </tr>

<% @attachments.each do |attachment| %>
  <tr>
    <td><%=h attachment.name %></td>
    <td><%= link_to 'Summary', :action => 'summary', :id => attachment.id %></td>
    <td><%= link_to 'View', :action => 'view', :id => attachment.id %></td>
    <td><%= link_to 'Download', :action => 'download', :id => attachment.id %></td>
    <td><%= link_to 'Remove', :action => 'delete', :id => attachment.id, :confirm => 'Are you sure? You will need to update any content pages that show this attachment.' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New picture or attachment', :action => 'new' %>
